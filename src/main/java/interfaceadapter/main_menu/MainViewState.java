package interfaceadapter.main_menu;

public class MainViewState {

    // You can add fields later if the main menu needs state
    // (e.g., last selected option), but for now it is empty.

    @Override
    public String toString() {
        return "MainViewState{}";
    }
}
